# 判斷案例庫（judgment-cases）

目的：累積本專案「判斷錯了被糾正、或差點判斷錯」的實例。規範檔（AGENTS.md）教原則，
這裡放帶情境的實例——AI 代理在遇到相似情境時，套用案例比套用原則命中率更高。

**維護規則**：每次發生「模型判斷錯誤被使用者糾正」或「合理假設被驗證推翻」，就加一條。
一條案例 = 情境 + 當時的判斷 + 裁決結果 + 可轉移的教訓。有數字就附數字，禁止事後美化。

---

## 案例 1：合理的假設也會被回測否決（Adaptive VOL_TARGET，2026-07-02）

- **情境**：假設曝險模型長期低估水位（premise：實現波動率中位數約 27%），提出用滾動 1 年波動率分位數自適應調整 VOL_TARGET。
- **當時的判斷**：邏輯上說得通，實作也完成了。
- **裁決**：3y/5y 回測——台股 Sharpe 只有 1.82→1.87 / 1.30→1.33 的微幅改善，美股反而劣化 1.43→1.37 / 0.65→0.58；且前提本身就是錯的（滾動中位數實際落在 15–20%，不是 27%）。功能保留在 `VOL_TARGET_ADAPTIVE=False` 開關後，不啟用。
- **教訓**：「邏輯合理 + 實作完成」不構成採納理由，回測數字才是裁決。**否決的實驗要把結論記錄在決策點**（config 註解、commit message），防止未來的代理或人重提同一假設。重新驗證用 `tests/backtest_exposure_3y.py --compare`。

## 案例 2：別的市場的結論不能直接外推（台股頂部排序，2026-07-02）

- **情境**：美股回測已證明高周轉排序損害 Sharpe（納入短期訊號 1.41→1.14），據此推論台股「按當日漲幅排序取頂部」也該改成按總分排序。
- **當時的判斷**：既然美股如此，台股照改即可——看起來不用再驗。
- **裁決**：`tests/backtest_tw_ranking.py` 用生產版評分逐週重演 45 個評分日：現行的漲幅排序在 5/10/20 日全部視窗都勝過總分排序（例：20 日 12.8% vs 11.0%）。現行邏輯保留。
- **教訓**：同一假設在不同市場（策略、資料頻率、參與者結構都不同）必須各自回測。**「在 A 成立」只是「在 B 值得一驗」的理由，不是「在 B 直接改」的理由。**

## 案例 3：回測異常先驗資料、再驗邏輯（cache 過期）

- **情境**：回測結果與預期不符時，直覺反應是調參數或改邏輯。
- **正確順序**：`src/advisor/cache/` 的 pkl 帶日期後綴（如 `hist_2y_20260703.pkl`），先確認腳本讀到的是當期 cache、資料區間涵蓋預期範圍，再去動邏輯。資料錯，後面全部白做。
- **教訓**：診斷順序固定為 **資料 → 讀取 → 邏輯 → 參數**。連調兩次參數無效，就該回頭驗資料（見 AGENTS.md「方向錯了的訊號」）。

## 案例 4：全 0 不等於成功——靜默過濾比報錯更危險（taifex API，2026-07-05）

- **情境**：期交所 OpenAPI 整合上線後，每日快取的三大法人淨額全為 0，`source` 標著 `taifex_api`，看起來像「API 成功但市場剛好沒部位」。
- **當時的判斷**：整合 commit 驗過「API 有回應、程式沒報錯」就視為完成。
- **裁決**：實測 API 發現 `ContractCode` 實際值是 `臺股期貨`（繁體臺、全名），程式碼寫 `台指期`——比對永遠不成立，所有記錄被靜默濾掉，加總得 0。真實數字是投信 +67,213、外資 -81,052，期貨策略的籌碼腿自整合以來完全失效。修復：改對契約名、零匹配時走 fallback 並印出 API 實際的契約清單、fallback 結果不寫快取。
- **教訓**：**驗證外部 API 整合時，「有回應、沒報錯」不夠，必須驗到「拿到非零／非空的業務數值」**。過濾條件對不上時的預設行為（空集合→加總 0）會把失敗偽裝成成功；寫解析代碼時要讓「一筆都沒匹配」顯式報警，而不是安靜地回傳中性值。

## 案例 5：用錯資料源比沒有資料源更難發現（融券端點，2026-07-05）

- **情境**：`fetch_margin_loan` 指向 TWSE `BFI82U`（三大法人買賣金額統計表），該表只有市場合計列、沒有個股融券欄位。解析器逐列過濾後永遠回傳空 DataFrame——不報錯、不拋例外。
- **當時的判斷**：函式有回應、有錯誤處理、看起來完整；下游 `screen_short.py` 把「查無融券」靜默處理為 0，於是條件 2（融券餘額 > 0）恆為 false，**生產版做空篩選器自上線起從未選出任何股票**，且無人察覺——輸出「今日無候選」與市況正常時的合法輸出無法區分。
- **裁決**：改用 `MI_MARGN`（信用交易統計，個股融資融券 16 欄），實測 2021/2022/2026 三個年代都有千餘檔資料（台積電 2022-10-05 融券餘額 462 張可交叉核對）。
- **教訓**：接外部 API 時，驗收標準不是「跑通」而是**「抽一筆已知數值與官方網頁對得上」**。以及：當一個篩選器長期輸出空結果，「市況不符」和「資料源壞了」外觀完全相同——要為「連續 N 日空結果」設計警訊，或在輸出中附上「各條件通過檔數」讓退化可見。

## 案例 6：中性／空頭改總分排序的假設被 5 年回測否決（台股頂部排序，2026-08-28）

- **情境**：週覆盤顯示盤差時選股命中率接近或低於股票池。假設是強勢股掃描在中性／空頭仍「只留今日上漲、按漲幅排」，把當日小跌的續強股排掉。提案：中性／空頭改為不過濾漲幅、按總分排（變體 C）；多頭維持現行（A）。
- **當時的判斷**：交易直覺合理；2 年快取暖機後幾乎都是多頭（中性 3 期、空頭 0），不夠裁決，需 5 年。
- **裁決**：`tests/backtest_tw_ranking.py --period 5y`（2022-09-21～2026-07-27，中性 53 期、空頭 14 期）。預先門檻：中性 T+5 報酬且 P−U 都贏 A 才改。Top10 T+5：A +2.21%／-1.6pp，B +2.14%／-2.7pp，C +2.07%／-0.8pp——C 只贏超額、輸報酬，沒過雙條件。Top3 更是 A 全贏。空頭 C 報酬較好看但僅 14 期（<30），不授權。3 年對照同向。**生產排序不改。**
- **教訓**：盤差時「不要追當天漲幅」是合理假設，仍要分段回測；贏超額或贏報酬其中一項不夠。空頭樣本少時維持現行、靠曝險縮部位，不要用 14 期改實盤。

---

## 案例 7：寬度崩跌先驗缺 K，再驗市場（美股曝險 85%→50%，2026-09-01）

- **情境**：日更警報「美股水位 85%→50%，波動率升至 10.9%」。指數仍多頭（7686 vs 季線 7542），VOO 綠燈，實現波動 10.9% 低於目標 15%（vol_scale=1）。
- **當時的判斷**：三因子公式算出來就推播，當內部結構真的垮了。
- **裁決**：cache 顯示 8/28 Yahoo 缺 465/520 檔收盤；`rolling(60).mean()` 預設 min_periods=60，8/31 只有 55 檔有 MA60。(close > NaN) 全 False，寬度 5.2%。同一批價格用 min_periods=50 + 最多 2 日前向填補後，寬度 52.1%（8/27 為 56.9%），水位約 80%，不是 50%。警報文案還把原因寫成波動率，與 vol_scale=1 矛盾。
- **教訓**：寬度單日掉超過幾十個百分點、但指數與波動幾乎沒動，先查「有多少檔 MA 有效」，不要先當風險訊號。缺 K 的修復在 `breadth_series`（ffill + min_periods + 分母排除 MA=NaN），不要手改 `latest_us.json`。

---

## 新增案例模板

```markdown
## 案例 N：一句話標題（主題，YYYY-MM-DD）

- **情境**：當時面對什麼問題、有什麼背景
- **當時的判斷**：模型（或人）原本怎麼判斷、為什麼那樣想
- **裁決**：實際驗證的結果，附數字或輸出
- **教訓**：可轉移到未來相似情境的一句話判準
```
